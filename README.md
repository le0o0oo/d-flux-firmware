# D Flux acquisition firmware
This is the code used in the ESP32 that connects via bluetooth to the app

in case clangd acts dumb:
1. try to restart clangd language server
if it didnt work, run command
```bash
pio run --target compiledb
```
and restart clangd language server again